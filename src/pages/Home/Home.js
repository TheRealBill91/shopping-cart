import { NavBar } from "../../components/NavBar";
import { Header } from "../../components/Header";

export const HomePage = () => {
    return (
        <>
            <Header />
            <h2>Welcome Home!</h2>
        </>
    );
};
